extends _layouts/_default

block title

  title #{site.name} — #{site.title}

block main

  div(class='container')

    p #{site.description}

    :markdown
      [Styleguide](styleguide.html), [Grid](grid.html)
